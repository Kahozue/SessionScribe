import SwiftUI
import SSUI

@main
struct SessionScribeApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
        .defaultSize(width: 1100, height: 720)
    }
}
